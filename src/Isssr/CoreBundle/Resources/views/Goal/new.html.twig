{% extends 'IsssrCoreBundle::layout.html.twig' %}

{% block pagetitle %}New Goal{% endblock %}
 
{% block content %}

<form action="{{ path('goal_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
    <p>
    	<a href="{{ path('goal') }}">
            <button>Back</button>
        </a>
    </p>
</form>

{% endblock %}