<h1>SuperInGoal list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Superemail</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('superingoal_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.superEmail }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('superingoal_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('superingoal_new', {'id': goalid}) }}">
            Create a new entry
        </a>
    </li>
</ul>
