<h1>SuperInGoal creation</h1>

<form action="{{ path('superingoal_create', {'id': goalid}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>
