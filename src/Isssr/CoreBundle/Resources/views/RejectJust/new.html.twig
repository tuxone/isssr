<h1>RejectJust creation</h1>

<form action="{{ path('rejectjust_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('rejectjust') }}">
            Back to the list
        </a>
    </li>
</ul>
